# ============================================================
# requirements.txt
# Install: pip install -r requirements.txt
# ============================================================

# ── Data Collection ──────────────────────────────────────────
google-generativeai>=0.8.0      # Gemini API (opsional, sudah diganti Groq)
groq>=0.11.0                    # Llama via Groq API (collect_gpt_oss.py)
openai>=1.40.0                  # GPT-4o-mini (collect_openai.py)
python-dotenv>=1.0.0            # Load API keys dari .env

# ── Data Processing ──────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
datasets>=2.14.0                # HuggingFace datasets (IndoNLI)
tqdm>=4.66.0

# ── Deep Learning ─────────────────────────────────────────────
torch>=2.0.0                    # BiLSTM
torchvision>=0.15.0             # opsional

# ── Transformer / IndoBERT ───────────────────────────────────
transformers>=4.38.0            # IndoBERT fine-tuning
accelerate>=0.27.0              # HuggingFace Trainer backend
sentencepiece>=0.1.99           # Tokenizer IndoBERT

# ── FastText (BiLSTM embeddings) ─────────────────────────────
fasttext-wheel>=0.9.2           # FastText ID embeddings (cc.id.300)

# ── Visualization & Reporting ────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0

# ── File Format Support ──────────────────────────────────────
pyarrow>=12.0.0                 # Parquet support (fetch_human.py fallback)